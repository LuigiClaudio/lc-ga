{
  "name": "@lc-ga/root",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "build": "yarn && cd site && yarn gatsby build"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "site"
    ]
  }
}
